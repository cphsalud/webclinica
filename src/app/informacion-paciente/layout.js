export const metadata = {
    title: 'Informacion al paciente | Clinica CPH Salud',
    description: 'Clinica CPH Salud, Informacion al paciente',
}

export default function InformacionPacienteLayout({
    children,
}) {
    return (
        <section>
            {children}
        </section>
    );
}