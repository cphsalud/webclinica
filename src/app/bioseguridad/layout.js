export const metadata = {
    title: 'Bioseguridad | Clinica CPH Salud',
    description: 'Clinica CPH Salud, Bioseguridad',
}

export default function BioseguridadLayout({
    children,
}) {
    return (
        <section>
            {children}
        </section>
    );
}